# ── Credentials & secrets ─────────────────────────────────────────────────────
.env
.env.*
env
env.developer
*.token
token.json
credentials.json
.user_cloud_tokens.json
user_cloud_tokens.json
secret_key
.secret_key

# ── Runtime state (contain real paths, tokens, backup history) ────────────────
config.json
history.json
backup_queue.json
*.sha256

# ── SFTP known-hosts (auto-generated, machine-specific) ───────────────────────
.backupsys_known_hosts

# ── Backup data & snapshots ───────────────────────────────────────────────────
backups/
snapshots/
logs/

# ── API server runtime data ───────────────────────────────────────────────────
backupsys.db
backupsys_files/

# ── Build artefacts ───────────────────────────────────────────────────────────
dist/
build/
*.spec
*.exe

# ── Temporary / auto-generated files ─────────────────────────────────────────
*.tmp

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/

# ── OS noise ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
config.backup.json
config.backup.*.json
icon.ico
